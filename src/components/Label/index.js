import React from 'react';
import Label from './Label'

// Pass in props that detail what you are trying to filter by

export default class FilteredByText extends React.Component {
    render() {
        return (
            <Label>City</Label>
        );
    }
}